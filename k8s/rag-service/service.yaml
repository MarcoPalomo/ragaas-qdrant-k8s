---
apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: ragaas
  labels:
    app.kubernetes.io/name: rag-service
    app.kubernetes.io/part-of: ragaas-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
