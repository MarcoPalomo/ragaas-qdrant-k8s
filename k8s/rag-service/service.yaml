apiVersion: v1
kind: Service
metadata:
	name: rag-service
	namespace: ragaas
	labels:
		app: rag-service
spec:
	type: ClusterIP
	selector:
		app: rag-service
	ports:
		- port: 8000
			targetPort: 8000
			protocol: TCP
			name: http
